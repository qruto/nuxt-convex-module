# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files. `npx convex dev` writes CONVEX_DEPLOYMENT and CONVEX_URL to
# .env.local — yours, not the example's.
.env
.env.*
!.env.example

# NOT ignored, on purpose: convex/_generated. Committing the codegen is what
# lets the example type-check and build before `npx convex dev` has ever run.
