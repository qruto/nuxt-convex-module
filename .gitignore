# Dependencies
node_modules
pnpm-debug.log*

# Logs
*.log*

# Temp directories
.temp
.tmp
.cache

# Generated dirs
dist
website/convex/_generated

# Nuxt
.nuxt
.output
.data
.vercel_build_output
.build-*
.netlify

# Env
.env
.env.*
!.env.example

# Testing
reports
coverage
*.lcov
*.junit.xml
.nyc_output

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Intellij idea
*.iml
.idea

# Build artifacts
*.tsbuildinfo
*.tgz

# Bundle analysis (Sonda)
.sonda

# deepsec security-scanner workspace (kept local-only: its INFO.md maps the
# repo's security-sensitive surface and exported findings describe unfixed
# vulnerabilities — neither belongs in a public repo)
.deepsec

# OSX
.DS_Store
.AppleDouble
.LSOverride
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# fallow's analysis cache, plus the files its GitHub Action writes into the
# workspace root: results.json, results.sarif, stderr.log, analysis-args.sh and
# the PR diff. None of them belong in the tree. Until they were ignored, `typos`
# scanned them in ci and failed on a random hex id inside one.
.fallow/
fallow-*
