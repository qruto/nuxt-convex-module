<template>
  <!-- The eight stops of the wordmark GRAPHITE ramp, shared by both brand marks
       so the two badges are always cut from the same metal (the relief note in
       BrandConvex.vue explains the curve). These colours are the static base;
       on the hero billet landing.css restyles the same stops per colour scheme
       BY POSITION (`stop:nth-of-type(n)`), so the count and order here are
       load-bearing. -->
  <stop offset="0" stop-color="#717579" />
  <stop offset="0.14" stop-color="#676b6f" />
  <stop offset="0.3" stop-color="#5d6165" />
  <stop offset="0.44" stop-color="#54575b" />
  <stop offset="0.58" stop-color="#4a4d51" />
  <stop offset="0.72" stop-color="#414448" />
  <stop offset="0.86" stop-color="#34373b" />
  <stop offset="1" stop-color="#3d4044" />
</template>
